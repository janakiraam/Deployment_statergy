var fullName = "Janaki Raam";

// how to declare a variable

// fullName
// full-Name
// full_Name
// FullName

var courseName = "Recat JS Bootcamp";
var isLoggedIN = true;

// ture and false is boolean variable and different from True and False

var loggedCount = 34;

// console.log(loggedCount);
// console.log("loggedCount");

var paymentMode;

console.log(paymentMode);

paymentMode = "Credit card";

// var if=23; ==> can't use as if is a reserved keyword